.DS_Store
.env
/node_modules/

# React Router
/.react-router/
/build/

/generated/prisma

# Nested git worktree — has its own .git file, not meant to be tracked as plain files
.claude/worktrees/
.claude/settings.local.json

# Local OpenWolf tooling state — not shared via the repo
.wolf/
